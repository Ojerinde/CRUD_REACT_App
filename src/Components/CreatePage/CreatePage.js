import React from "react";

import Card from "../UI/Card/Card";
import Header from "../UI/Header/Header";

const CreatePage = (props) => {
  return (
    <>
      <Header></Header>
      <Card>
        <p>I am at create page</p>
      </Card>
    </>
  );
};
export default CreatePage;
